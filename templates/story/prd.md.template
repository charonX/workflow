# {{STORY_NAME}}

> 状态：探索期 / 结晶中 / 已锁定
> 故事 ID：`{{STORY_ID}}`
> 最后更新：{{DATE}}

---

## 1. 问题陈述

<!-- 用户感受到的痛点，从用户视角写。 -->

## 2. 解决方案

<!-- 我们要给用户什么解决方案，一句话 + 一段话。 -->

## 3. 用户故事

1. 作为 <角色>，我想要 <能力>，以便 <收益>。
2. ...

## 4. 稳定块（已稳定，可结晶为 REQ）

| # | 稳定块 | 为什么不再推翻 |
|---|---|---|
| 1 | | |

## 5. 移动块（还在动，暂不入 REQ）

| # | 还在动的块 | 不确定什么 |
|---|---|---|
| 1 | | |

## 6. 用户操作流（Operation Flows）

<!-- 强制：每个稳定块至少一条 happy path；complex story 必须列出分支与异常；每个稳定块至少 1 条预期值锚点（§6.3）。 -->

### 6.1 主流程 / Happy Path

| 步骤 | 用户动作 | 系统响应 | 验收锚点 |
|---|---|---|---|
| 1 | | | |

### 6.2 分支与异常

| 触发条件 | 分支结果 | 对应错误状态 |
|---|---|---|
| | | |

### 6.3 预期值锚点（Expected-Value Anchors）

<!-- 强制：每个稳定块至少给出 1 条具体的"输入 → 预期输出/结果"例子，值必须是机器可验的字面值（数字、字符串、文件内容、exit code、JSON 片段等）。这是 test-author 推导断言 expected 值的唯一来源；抽象描述（如"显示成功提示"）不算锚点。 -->

| 稳定块 | 输入 | 预期输出/结果 | 依据（需求 / 已签标准 / 真实 JSON） |
|---|---|---|---|
| 1 | | | |

## 7. 表单与输入验证（Form / Input Validation）

<!-- 每个接受用户输入的稳定块必须填写。无用户输入则写"本 story 无用户输入"。每条规则至少给 1 个有效例子 + 1 个无效例子，供 test-author 推导 expected 值。 -->

| 输入字段 | 规则 | 有效例子 | 无效例子（→错误提示） | 错误状态 |
|---|---|---|---|---|
| | | | | |

### 7.1 跨字段/业务规则

| 规则 | 触发时机 | 例子（触发 → 期望结果） | 错误状态 |
|---|---|---|---|
| | | | |

## 8. 错误状态与失败响应（Error States / Failure Responses）

<!-- 强制：列出所有可预见的失败模式。无外部依赖/无失败模式则显式声明 N/A。 -->

| 场景 | 触发条件 | 错误码/消息 | 用户可见状态 | 副作用/回滚 |
|---|---|---|---|---|
| | | | | |

## 9. 复杂度分级

| 维度 | 取值/说明 |
|---|---|
| 复杂度 | simple / complex |
| 判断理由 | 模块数、输入量、分支数、外部依赖数 |

- `simple`：可减少表格粒度，但第 8 节“错误状态”不可跳过；第 10 节“技术方案”高层概括即可。
- `complex`：必须完整填写第 6-8 节；第 10 节“技术方案”必须完整，由 `/tech-design` 深潜产出（本 story 的结晶路径为 `PRD → … → TECH-DESIGN(条件) → CRYSTALLIZE`）。

## 10. 技术方案（Implementation Decisions）

<!-- 不写文件路径，只到模块/接口/数据流层。simple story 高层概括；complex story 必须由 /tech-design 完整填充。 -->

### 10.1 设计目标

<!-- 用一句话概括本方案要解决的系统级问题。 -->

-

### 10.2 模块与边界

<!-- 列出本 story 涉及或新增的模块/服务/executive，以及各自职责。 -->

| 模块 | 职责 | 是否新增 |
|---|---|---|
| | | 是 / 否 |

#### 模块关系图

```
[用户输入]
   │
   ▼
[模块 A] ──> [模块 B] ──> [副作用 / 外部系统]
   │
   ▼
[模块 C]
```

### 10.3 数据流

<!-- 描述关键用户操作或事件触发后，数据如何流经系统。 -->

1. **触发**：...
2. **输入校验**：...
3. **核心处理**：...
4. **副作用**：...
5. **输出**：...

### 10.4 接口契约

<!-- 模块间或对外暴露的关键接口。跨模块 REQ 必须在此定义契约。每个契约至少给 1 组具体样例（golden values），供 test-author 推导 expected 值。 -->

#### 接口名称：...

| 项目 | 说明 |
|---|---|
| 调用方 | |
| 被调用方 | |
| 输入 | |
| 输出 | |
| 业务错误 | |
| 系统错误 | |
| 副作用 | |
| 幂等性 | 是 / 否 |

**样例（golden values）**：

| 场景 | 请求/输入 | 期望响应/输出 |
|---|---|---|
| 正常 | | |
| 边界 | | |
| 异常 | | |

### 10.5 关键决策

<!-- 技术选型、算法、依赖引入等。满足 ADR 三条件（难逆转、不说明会令人困惑、有真实取舍）时，必须同步写入 `adr/NNNN-<title>.md`，并更新 `adr/README.md`。 -->

| 决策 | 选项 | 选择理由 | 风险 |
|---|---|---|---|
| | | | |

### 10.6 风险与回流点

<!-- 哪些技术假设如果错了，需要回流到哪一层。 -->

| 假设 | 如果错了会怎样 | 回流到 | 能否快速验证 |
|---|---|---|---|
| | | PRD / TECH-DESIGN | 能 / 不能 |

### 10.7 安全/性能/可观测性

<!-- 涉及信任边界/资产时参考 `checklists/security.md` 做威胁建模；潜在瓶颈参考 `checklists/performance.md`；新关键路径参考 `checklists/observability.md` 预留 telemetry seams。 -->

-

## 11. 测试决策（Testing Decisions）

### 11.1 覆盖接缝（coverage seams，CLI 优先）

<!-- 每个稳定块至少对应一个 seam；跨模块的块需要接口级测试。产品 CLI 是默认 seam，能 CLI 化的行为不进浏览器 E2E。 -->

| 稳定块 | Seam | 测试类型 | 依赖处理 |
|---|---|---|---|
| | | 单元 / 集成 / E2E | mock / 真实 / stub |

### 11.2 测试策略与先例

<!-- 描述什么构成一个好的测试（只测外部行为，不测实现细节）；测试哪些模块；仓库里相近的先例。 -->

-

## 12. 范围外

-

## 13. 补充说明

-

## 14. PRD 完整性自检查

| 检查项 | 状态 | 备注 |
|---|---|---|
| 操作流 | PASS / GAP | |
| 输入验证 | PASS / GAP | |
| 错误状态 | PASS / GAP | |
| 预期值锚点 | PASS / GAP | 每个稳定块 ≥1 条具体例子（§6.3）；输入验证有有效/无效例子（§7） |
| 复杂度分级 | simple / complex | |
| 技术方案（§10） | PASS / GAP / N/A | complex 必须完整，simple 可高层 |

> 任何 GAP 项必须归类，不许悬空：**就地补** / 归入移动块（§5）/ **新建 story** / 归入范围外（§12）。`/crystallize` 会对话确认归类而非阻断。

---

## 版本记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v0.1 | {{DATE}} | 初稿 | AI + 人 |
