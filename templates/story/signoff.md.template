# 签核记录 — {{STORY_NAME}}

> 故事 ID：`{{STORY_ID}}`
> 生成日期：{{DATE}}

本文件由 `/tac-signoff` 维护，包含两个阶段：
- **Assertion** — 实现前签核功能断言。
- **Feel** — 实现后验收视觉与交互观感。

---

## Assertion 阶段

### 签核声明

我确认：本 story 所有 REQ 的 expected 值均来自我对需求的理解，或从已签字的 REQ 中机械推出。**绝非**从当前代码输出抄写。

| REQ-ID | 断言内容摘要 | 测试位置 |
|---|---|---|
| REQ-P0-001 | | |
| REQ-P0-002 | | |
| ... | | |

### 检查清单

- [ ] 每个 REQ-ID 都有对应测试。
- [ ] 每个测试文件都有 `// REQ-TRACE` 和 `// REQ-VERSION`。
- [ ] 无 `// TODO: HUMAN ASSERTION` 占位。
- [ ] expected 值来源清晰（人手算 / 已签标准 / 真实 JSON）。
- [ ] 无 snapshot 当测试预言。
- [ ] 边界/错误场景已覆盖。

⏸ **BUILD 阶段在 `[tac-test] assertion-signoff for {{STORY_ID}}` commit 前不得开始。**

---

## Feel 阶段

### 总体裁决

- [ ] 通过 — 实现符合 HTML UX 参照与 REQ，可合并。
- [ ] 已提缺陷 — 发现偏差，已分类为缺陷，回流 REQ。
- [ ] REQ 变更 — 发现 REQ 覆盖不足，需要更新 REQ。

### 偏差记录

| # | 描述 | HTML UX 参照 | 分类 | 关联 REQ-ID | 处理方式 |
|---|---|---|---|---|---|
| 1 | | | 缺陷 / 需求变更 / 可接受偏差 | | 更新 REQ → 测试 → 实现 / 记录并放行 |
| 2 | | | | | |

**分类说明：**
- **缺陷**：实现偏离已签 REQ，说明 REQ 验收标准漏了此场景 → 补标准增量。
- **需求变更**：REQ 没写或写错 → 修改 REQ。
- **可接受偏差**：HTML 原型无法 1:1 翻译（平台限制等）→ 记录并放行。

### 检查清单

- [ ] App / 产品在目标环境启动无崩溃。
- [ ] 关键用户流程可走完。
- [ ] 视觉层面对照 HTML UX 参照：结构、元素顺序、颜色、排版、间距、动效。
- [ ] 无系统错误弹窗 / 空白页。
- [ ] “陈旧” / 错误 / 降级状态表达温和、不焦虑。
- [ ] implementer 报告的 HTML 偏差已被确认。

⏸ **Feel 签核通过前禁止合并到主分支。**
