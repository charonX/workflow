// REQ-TRACE: REQ-{{PHASE}}-001, REQ-{{PHASE}}-002
// REQ-VERSION: {{REQ_VERSION}}
// TEST-AUTHOR: agent
// ASSERTIONS-SIGNED: false

import XCTest
@testable import {{MODULE_NAME}}

final class {{TEST_CLASS_NAME}}: XCTestCase {

    // MARK: - REQ-{{PHASE}}-001

    func test{{BehaviorName}}() {
        // 给定
        let input = /* 脚手架填充 */

        // 当
        let result = /* 脚手架填充 */

        // 则
        // TODO: HUMAN ASSERTION — 替换为已签核的 expected 值
        XCTAssertEqual(result, <#expected#>)
    }

    // MARK: - REQ-{{PHASE}}-002

    func test{{EdgeCaseName}}() {
        // TODO: HUMAN ASSERTION
    }
}
